# ===========================================
# PLANTILLA DE VARIABLES DE ENTORNO
# ===========================================
#
# INSTRUCCIONES:
# 1. Copia este archivo a .env.local
# 2. Remplaza los valores de ejemplo con tus valores reales
# 3. NUNCA comitees .env.local (ya está en .gitignore)
#
# ===========================================

# Perfil de Spring (dev, test, prod)
SPRING_PROFILE=prod

# ===========================================
# AZURE SQL DATABASE
# ===========================================
# Obtén tu connection string desde:
# Azure Portal > Tu SQL Database > Settings > Connection strings
#
# Formato: jdbc:sqlserver://[server-name].database.windows.net:1433;database=[db-name];encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
DB_URL=jdbc:sqlserver://tu-servidor.database.windows.net:1433;database=tu-base-datos;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;

# Usuario administrador de SQL (generalmente sqladmin)
DB_USERNAME=sqladmin

# Contraseña del usuario administrador
# ⚠️ IMPORTANTE: Esta contraseña debe ser segura y cumplir con los requisitos de Azure
DB_PASSWORD=TuPasswordSegura123!

# Driver de SQL Server (no cambiar)
DB_DRIVER=com.microsoft.sqlserver.jdbc.SQLServerDriver

# ===========================================
# JPA/HIBERNATE
# ===========================================
# DDL Auto: validate (producción), update (desarrollo), create-drop (testing)
DDL_AUTO=validate

# Mostrar SQL en logs (true para desarrollo, false para producción)
SHOW_SQL=false

# Dialecto de Hibernate para SQL Server (no cambiar)
HIBERNATE_DIALECT=org.hibernate.dialect.SQLServerDialect

# ===========================================
# JWT (JSON Web Token)
# ===========================================
# Secret para firmar tokens JWT
# Genera uno seguro con: openssl rand -base64 32
# O en PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
JWT_SECRET=tu-secret-jwt-super-seguro-base64

# Expiración del token en milisegundos (86400000 = 24 horas)
JWT_EXPIRATION=86400000

# ===========================================
# SERVER
# ===========================================
# Puerto del servidor (8080 por defecto)
PORT=8080
